# 🧠 RedPulse ML Training Strategy (Asymmetric Zero-Day Architecture)

본 문서는 RedPulse의 AI 코어를 고도화하고 초대형 엔터프라이즈 환경에서 데이터센터 노드의 무부하(Zero-Overhead) 예측을 실현하기 위한 고유의 **분산-비대칭 AI 아키텍처(Asymmetric AI Architecture)** 전략을 정의합니다.

---

## 1. Zero-Day 가상 모사 사전 학습 체계 (Heavy Offline)

전통적인 가속 파괴 시험 혹은 장기간의 실제 관측에 의존하는 선행 기술들의 한계를 극복하기 위해, 라이브 서버가 가동되기 전(Zero-Day)에 무거운 계산 부하를 전부 소진하는 전략을 취합니다.

### 1-1. S/W-H/W 티어링 기반 몬테카를로 데이터 합성
오프라인 `synthetic_generator.py`를 통해 수만 건의 가상 수명 시나리오를 제조합니다. 
- **OS 캐시 정책 반영**: Linux `dm-cache`의 정책(Write-back vs Write-through) 및 히트율(Hit-Ratio) 곡선이 타겟 낸드의 수명을 어떻게 극적으로 보호/감산시키는지 비선형 마모 공식을 통해 수학적으로 생성합니다.
- **NAND 물리 제원 반영**: TLC vs QLC의 본질적인 Write Amplification 민감성 차이를 기초 변수로 포함.

### 1-2. 무한한 가상 템플릿(Base Trajectory)의 생성
과거 데이터 없이도 상기 물리적 규칙 기반의 무작위 워크로드(랜덤 I/O 비율 등) 조합을 통해 시계열 (WAF 변동 - Hit Ratio - RUL) 로그 템플릿 집합 거미줄을 형성합니다.

---

## 2. 라이브 텔레메트리 매칭 추론망 (Light-weight Online)

온라인(운영 중인 데이터센터 랙 서버) 환경에서는 복잡한 물리 연산을 단 1건도 수행하지 않습니다. 

### 2-1. LSTM/임베딩 기반 단발적 숏컷 매칭
- **목표**: 에이전트로부터 전달받은 극히 짧은 시계열 스니펫(예: 과거 1~2일 간의 WAF 및 캐시 히트율)만을 입력값으로 받습니다.
- **연산**: 이 짧은 스니펫을 오프라인에서 사전 구축된 거대한 가상 템플릿 공간(Latent Space)과 비교하여 타겟 디바이스가 10만 개의 궤적 중 어디를 달리고 있는지 **최근접 매칭(O(1) 수준 복잡도)**을 수행합니다.
- **부하 억제 (Asymmetric Isolation)**: 해당 추론 렌즈 모듈(`live_inference.py`)은 오프라인의 무거운 물리 시뮬레이터 객체를 전혀 임포트하지 않도록 파일 단위로 엄격히 분리하여 설계되었습니다. 

---

## 3. 동적 연합 학습 모델 (Federated Calibration)

시간이 흘러 실측된 장기 데이터가 다수 확보되었을 때를 대비한 전략입니다.

### 3-1. 마이크로 그레디언트 전송
보안이 엄격한 온-프레미스 기업 고객 서버에서 S.M.A.R.T 데이터나 로우-레벨 I/O 로그를 직접 반출하지 않습니다. 오직 '사전 예측된 중앙값 대비 어긋난 오차의 Gradient(기울기 벡터)'만을 익명화하여 쏴줍니다.

### 3-2. 글로벌 가상 모사 엔진의 재조정 (Meta-Tuning)
RedPulse 중앙 시스템의 거대한 오프라인 시뮬레이터 망은 이 각지의 그래디언트들을 통합하여 베이스라인 물리 공식(TLC 랜덤쓰기 패널티 계수 등)을 아주 미세하게 보정하고, 이를 기반으로 다시 더욱 정교해진 합성 데이터 세트를 재배포하여 모델의 영점 조절을 수행합니다.

---
