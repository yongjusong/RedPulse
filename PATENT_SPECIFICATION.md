# 특허 출원 명세서 (초안)

## 발명의 명칭

**합성 데이터 사전 학습과 단기 실측 텔레메트리의 융합에 기반한 다계층 SSD 잔여 수명 외삽 예측 장치 및 방법**

(영문) Method and Apparatus for Extrapolative Prediction of Multi-Layered SSD Remaining Useful Life Based on Fusion of Synthetic Data Pre-training and Short-term Telemetry Observation

---

## 기술 분야

본 발명은 반도체 기반 저장장치(Solid State Drive, SSD)의 수명 예측 분야에 관한 것으로, 보다 상세하게는 NAND 플래시 메모리의 물리적 마모 특성을 소프트웨어로 모사(Simulation)하여 대규모 합성 노후화 데이터(Synthetic Degradation Dataset)를 자동 생성하고, 이를 기계 학습(Machine Learning) 모델에 사전 학습(Pre-training)시킨 후, 실제 운용 환경에서 단시간 내 관측된 소량의 텔레메트리(Telemetry) 데이터를 상기 사전 학습된 모델에 투입하여 저장장치의 잔여 수명(RUL, Remaining Useful Life)을 외삽(Extrapolation) 추론하는 장치 및 그 방법에 관한 것이다.

---

## 배경 기술

### 종래 기술의 한계

기존의 SSD 수명 예측 기술은 크게 두 가지 접근법으로 나뉜다.

**첫째, 물리적 마모 시험 방식.** 실제 저장장치에 장기간(수개월~수년) 워크로드를 인가하여 S.M.A.R.T.(Self-Monitoring, Analysis and Reporting Technology) 로그를 축적한 뒤 선형 회귀 등으로 잔여 수명을 추정하는 방식이다. 이 방식은 실제 데이터에 기반하므로 정확도가 높을 수 있으나, (a) 장비의 물리적 마모가 불가피하고, (b) 수개월 이상의 관측 시간이 필요하며, (c) 테스트 대상 장비 수만큼 비용이 선형적으로 증가하는 근본적인 한계가 있다.

**둘째, 순수 수학적 시뮬레이션 방식.** 저장장치의 P/E(Program/Erase) 사이클 한계값과 일일 기입량(DWPD)을 단순 나눗셈하여 예상 수명을 산출하는 방식이다. 이 방식은 빠르지만, (a) 실제 운용 환경에서 발생하는 쓰기 증폭(Write Amplification Factor, WAF)의 비선형적 변화를 반영하지 못하고, (b) Device-Mapper 기반의 캐시 계층(예: dm-cache)이 적용된 하이브리드 스토리지 아키텍처에서의 캐시 적중(Cache Hit)에 따른 수명 연장 효과를 고려할 수 없으며, (c) 랜덤 쓰기 비율에 따른 가비지 컬렉션(Garbage Collection) 부하 증가를 정밀하게 반영하지 못하는 한계가 있다.

### 해결하고자 하는 과제

본 발명은 상기한 종래 기술의 문제점을 해결하기 위한 것으로, 실제 저장장치를 장기간 파괴적으로 시험하지 않으면서도, 단시간(1~2시간)의 초기 관측 데이터만으로 다계층(TLC, QLC) 및 하이브리드 캐시 구성 환경에서의 장기적 수명 궤적을 높은 신뢰도로 외삽 예측할 수 있는 장치 및 방법을 제공하는 것을 목적으로 한다.

---

## 발명의 상세한 설명

### 1. 전체 시스템 구성 (도면 1 참조)

본 발명에 따른 SSD 잔여 수명 외삽 예측 장치(이하 "RedPulse 장치")는 다음의 구성 요소를 포함한다.

**(a) 사용자 인터페이스부 (User Interface Module)**
- 웹 기반 대시보드로 구현되며, 사용자가 저장장치의 물리적 제원(용량, NAND 셀 유형, P/E 사이클 한계)과 워크로드 프로파일(일일 기입량, 랜덤/순차 비율)을 입력하는 가상 토폴로지 설정 수단을 포함한다.
- 또한 실제 운용 환경에서 단시간 측정된 텔레메트리 데이터(관측 WAF, 관측 캐시 적중률)를 입력하는 실측 데이터 입력 수단을 포함한다.
- 사용자가 **제1 모드(가상 시뮬레이션 모드)**와 **제2 모드(텔레메트리 외삽 모드)** 사이를 전환할 수 있는 모드 선택 수단을 포함한다.

**(b) API 게이트웨이부 (API Gateway Module)**
- 상기 사용자 인터페이스부로부터 수신한 요청을 분류하여, 제1 모드의 요청은 물리 시뮬레이션 엔진부로, 제2 모드의 요청은 기계 학습 외삽 추론부로 라우팅한다.

**(c) 물리 시뮬레이션 엔진부 (Physics-based Simulation Engine Module)**
- NAND 플래시 셀의 유형별(TLC: ~3000 P/E, QLC: ~1000 P/E) 마모 특성을 수학적으로 모사하는 **저장장치 물리 모델(Storage Physics Model)**을 포함한다.
- 상기 물리 모델은 랜덤 쓰기 시 QLC 셀에서 발생하는 비선형적 쓰기 증폭(WAF) 패널티를 차등 적용하는 가중치 연산 로직을 포함한다.
- Device-Mapper 기반 하이브리드 구성(예: SLC 캐시 티어 + QLC 백엔드)을 모사하는 **하이브리드 캐시 시뮬레이터**를 포함하며, 이는 캐시 적중률(Cache Hit Ratio)에 따라 백엔드 QLC 저장장치로 전달되는 실제 쓰기량을 동적으로 감산하는 로직을 구현한다.
- 가상 시간축(Virtual Timeline) 위에서 일 단위로 마모를 누적 연산하여, SSD 건강 상태(Health Percentage)가 0%에 도달할 때까지의 시계열 노후화 곡선 데이터를 출력한다.

**(d) 합성 데이터 생성부 (Synthetic Dataset Generator)**
- 상기 물리 시뮬레이션 엔진부를 반복적으로 호출하되, 매 호출마다 저장장치 유형(TLC/QLC/Hybrid), 용량, 일일 기입량, 랜덤 비율, 캐시 용량 등의 파라미터를 무작위로 조합하여 **대규모 합성 노후화 데이터셋**을 자동 생성한다.
- 각 시나리오의 결과로서, 초기 관측 가능한 특징값(WAF, 캐시 적중률)과 최종 수명 일수(Days_To_EOL)의 쌍(Pair)을 기록하여 학습 데이터로 축적한다.

**(e) 기계 학습 외삽 추론부 (ML Extrapolation Inference Module)**
- 상기 합성 데이터 생성부가 생성한 대규모 데이터셋으로 **사전 학습(Pre-training)**된 회귀 모델(Random Forest Regressor)을 포함한다.
- 상기 모델은 다음의 특징 벡터(Feature Vector)를 입력으로 수신한다:
  - 저장장치 유형 인코딩값 (drive_type_encoded)
  - 저장장치 용량 (capacity_gb)
  - 일일 기입량 (daily_writes_gb)
  - 랜덤 쓰기 비율 (random_ratio)
  - 캐시 계층 용량 (cache_size_gb)
  - 단기 실측 쓰기 증폭률 (observed_early_waf)
  - 단기 실측 캐시 적중률 (observed_early_hit_ratio)
- 상기 특징 벡터를 입력받아, 잔여 수명 일수(predicted_rul_days)를 출력한다.

**(f) 시각화 출력부 (Visualization Output Module)**
- 상기 물리 시뮬레이션 엔진부 또는 기계 학습 외삽 추론부의 출력 결과를 수신하여, 시간축(일/년) 대비 SSD 건강 상태(%)의 노후화 궤적을 인터랙티브 차트로 렌더링한다.
- 예측된 잔여 수명, 평균 쓰기 증폭률, 캐시 적중률 등의 핵심 지표를 수치 카드 형태로 동시에 표출한다.

---

### 2. 발명의 동작 방법 (도면 2, 도면 3 참조)

본 발명에 따른 SSD 잔여 수명 외삽 예측 방법은 다음의 단계를 포함한다.

**[Phase A] 합성 데이터 기반 사전 학습 단계**

- **(S100)** 물리 시뮬레이션 엔진부가 저장장치 유형, 용량, 워크로드 파라미터를 무작위로 조합한 N개(N >= 1,000)의 가상 시나리오를 생성하는 단계;
- **(S110)** 각 시나리오에 대해, 가상 시간축 위에서 일 단위로 쓰기 증폭(WAF)과 P/E 사이클 소모를 누적 연산하여 SSD 건강 상태가 0%에 도달할 때까지 시뮬레이션을 수행하는 단계;
- **(S120)** 각 시나리오의 초기 관측 특징값(WAF, 캐시 적중률)과 최종 수명 일수를 쌍으로 기록하여 합성 데이터셋을 구축하는 단계;
- **(S130)** 상기 합성 데이터셋을 학습 데이터와 검증 데이터로 분할하고, 회귀 모델(Random Forest Regressor)을 학습시켜 사전 학습된 모델 가중치를 저장하는 단계;

**[Phase B] 제1 모드 - 가상 시뮬레이션 수명 예측 단계**

- **(S200)** 사용자가 인터페이스부를 통해 제1 모드를 선택하고, 저장장치 제원 및 예상 워크로드 파라미터를 입력하는 단계;
- **(S210)** API 게이트웨이부가 요청을 물리 시뮬레이션 엔진부로 라우팅하는 단계;
- **(S220)** 물리 시뮬레이션 엔진부가 입력된 파라미터에 기초하여 시계열 노후화 곡선 데이터를 생성하고, 잔여 수명을 산출하는 단계;
- **(S230)** 시각화 출력부가 상기 결과를 노후화 궤적 차트 및 수치 지표로 렌더링하는 단계;

**[Phase C] 제2 모드 - 텔레메트리 기반 외삽 추론 단계**

- **(S300)** 사용자가 실제 운용 환경의 저장장치에서 단시간(예: 1~2시간) 가혹 시험(fio 등)을 수행하여 초기 텔레메트리 데이터(실측 WAF, 실측 캐시 적중률)를 획득하는 단계;
- **(S310)** 사용자가 인터페이스부를 통해 제2 모드를 선택하고, 상기 텔레메트리 데이터 및 저장장치 제원을 입력하는 단계;
- **(S320)** API 게이트웨이부가 요청을 기계 학습 외삽 추론부로 라우팅하는 단계;
- **(S330)** 기계 학습 외삽 추론부가 Phase A에서 사전 학습된 모델에 상기 텔레메트리 데이터를 특징 벡터로 투입하여, 잔여 수명(RUL)을 외삽 추론하는 단계;
- **(S340)** 시각화 출력부가 상기 외삽 결과를 시계열 노후화 궤적 차트 및 수치 지표로 렌더링하는 단계.

---

### 3. 발명의 효과

본 발명에 의하면, 다음의 효과를 달성할 수 있다.

1. **시간 및 비용 절감 효과**: 종래 수개월~수년이 소요되던 SSD 수명 검증 시간을 1~2시간의 초기 테스트 + 수 초의 AI 추론으로 단축함으로써, 검증 시간을 99% 이상 절감할 수 있다.

2. **비파괴 검증 효과**: 실제 저장장치를 수명 종료 시점까지 마모시킬 필요 없이, 극소량의 초기 마모만으로 전체 수명 궤적을 예측할 수 있어 장비 자산의 손실을 최소화한다.

3. **다계층 하이브리드 아키텍처 지원 효과**: Device-Mapper 기반 캐시 계층(dm-cache 등)이 적용된 하이브리드 스토리지 구성에서의 캐시 적중 효과에 따른 수명 연장분을 정량적으로 예측할 수 있어, 종래의 단일 디스크 대상 예측 기술로는 불가능했던 계층형 스토리지 아키텍처 설계 의사결정을 지원한다.

4. **합성 데이터 활용의 효과**: 대규모 합성 데이터셋을 물리 시뮬레이션 엔진으로 자동 생성하여 기계 학습 모델을 사전 학습시킴으로써, 실제 고장 데이터가 부족한 상황에서도 충분한 학습 데이터를 확보할 수 있다.

---

## 청구범위 (특허청구항)

### 청구항 1 (독립항 - 방법)
저장장치의 잔여 수명을 예측하는 방법에 있어서,
(a) NAND 플래시 메모리의 셀 유형별 마모 특성을 수학적으로 모사하는 물리 시뮬레이션 엔진을 이용하여, 복수의 무작위 파라미터 조합에 대한 가상 마모 시뮬레이션을 반복 수행함으로써 합성 노후화 데이터셋을 생성하는 합성 데이터 생성 단계;
(b) 상기 합성 노후화 데이터셋을 이용하여 기계 학습 회귀 모델을 사전 학습시키는 사전 학습 단계;
(c) 실제 운용 환경의 저장장치에서 소정 시간 이내의 단기 가혹 시험을 수행하여 초기 텔레메트리 데이터를 획득하는 실측 데이터 획득 단계; 및
(d) 상기 사전 학습된 기계 학습 모델에 상기 초기 텔레메트리 데이터를 투입하여 상기 저장장치의 잔여 수명을 외삽 추론하는 외삽 추론 단계;
를 포함하는 것을 특징으로 하는, 저장장치의 잔여 수명 예측 방법.

### 청구항 2 (종속항)
제1항에 있어서,
상기 물리 시뮬레이션 엔진은, Device-Mapper 기반 캐시 계층이 적용된 하이브리드 스토리지 구성을 모사하는 하이브리드 캐시 시뮬레이터를 포함하며, 상기 하이브리드 캐시 시뮬레이터는 캐시 적중률에 따라 백엔드 저장장치로 전달되는 실제 쓰기량을 동적으로 감산하는 것을 특징으로 하는, 저장장치의 잔여 수명 예측 방법.

### 청구항 3 (종속항)
제1항에 있어서,
상기 초기 텔레메트리 데이터는, 쓰기 증폭률(WAF) 및 캐시 적중률(Cache Hit Ratio)을 포함하고, 상기 단기 가혹 시험의 소요 시간은 2시간 이내인 것을 특징으로 하는, 저장장치의 잔여 수명 예측 방법.

### 청구항 4 (종속항)
제1항에 있어서,
상기 합성 데이터 생성 단계에서, 상기 물리 시뮬레이션 엔진은 QLC 셀에 대해 랜덤 쓰기 시 TLC 셀 대비 높은 쓰기 증폭 가중치를 차등 적용하는 것을 특징으로 하는, 저장장치의 잔여 수명 예측 방법.

### 청구항 5 (독립항 - 장치)
저장장치의 잔여 수명을 예측하는 장치에 있어서,
사용자로부터 저장장치 제원 및 워크로드 파라미터를 수신하는 인터페이스부;
NAND 플래시 셀의 유형별 마모 특성을 수학적으로 모사하여 시계열 노후화 곡선 데이터를 생성하는 물리 시뮬레이션 엔진부;
상기 물리 시뮬레이션 엔진부를 반복 호출하여 합성 노후화 데이터셋을 자동 생성하는 합성 데이터 생성부;
상기 합성 노후화 데이터셋으로 사전 학습된 기계 학습 모델을 포함하며, 단기 실측 텔레메트리 데이터를 입력받아 잔여 수명을 외삽 추론하는 기계 학습 외삽 추론부; 및
상기 물리 시뮬레이션 엔진부 또는 상기 기계 학습 외삽 추론부의 출력을 시각화하는 시각화 출력부;
를 포함하는 것을 특징으로 하는, 저장장치의 잔여 수명 예측 장치.

### 청구항 6 (종속항)
제5항에 있어서,
상기 인터페이스부는, 사용자가 가상 시뮬레이션 모드와 텔레메트리 외삽 모드를 전환할 수 있는 모드 선택 수단을 포함하고, API 게이트웨이부가 선택된 모드에 따라 요청을 상기 물리 시뮬레이션 엔진부 또는 상기 기계 학습 외삽 추론부로 분기 라우팅하는 것을 특징으로 하는, 저장장치의 잔여 수명 예측 장치.

---

## 도면의 간단한 설명

- **도면 1**: RedPulse 전체 시스템 컴포넌트 구조도 (System Architecture Diagram) -- 사용자 인터페이스부, API 게이트웨이부, 물리 시뮬레이션 엔진부, 기계 학습 외삽 추론부 간의 데이터 흐름을 도시한 블록도.
- **도면 2**: 시스템 데이터 흐름도 (Data Flow Diagram) -- 외부 엔티티(사용자)로부터 입력된 데이터가 각 처리 모듈을 거쳐 최종 출력으로 변환되는 논리적 정보 흐름을 도시한 DFD.
- **도면 3**: 머신러닝 파이프라인 동작 순서도 (Sequence Diagram) -- Phase 1(합성 데이터 생성 및 사전 학습)과 Phase 2(사용자 텔레메트리 입력 및 외삽 추론)의 시간순 동작 흐름을 도시한 시퀀스 다이어그램.

---

## 기존 기술 대비 차별점 요약 (선행 기술 비교 분석용)

| 비교 항목 | 종래 기술 A (물리적 장기 마모 시험) | 종래 기술 B (단순 수학적 산출) | 본 발명 (RedPulse) |
|---|---|---|---|
| 검증 소요 시간 | 수개월 ~ 수년 | 즉시 (수 초) | 1~2시간 실측 + 수 초 AI 추론 |
| 장비 물리적 손상 | 수명 종료까지 완전 마모 | 없음 | 극소량 초기 마모만 발생 |
| WAF 비선형성 반영 | 실측이므로 자동 반영 | 반영 불가 | 물리 모델 + ML 학습으로 반영 |
| 하이브리드 캐시 지원 | 가능하나 장기 시험 필요 | 지원 불가 | dm-cache 캐시 적중률 모사 및 예측 |
| 예측 신뢰도 | 높음 (실측 기반) | 낮음 (과도한 단순화) | 높음 (합성 사전 학습 + 실측 보정) |
| 데이터 부족 대응 | 대응 불가 (실측 필수) | 해당 없음 | 합성 데이터 무한 생성으로 해결 |

---

## 부록: 핵심 구현 파일과 명세서 구성 요소 간의 매핑

| 명세서 구성 요소 | 대응 소스 코드 |
|---|---|
| 물리 시뮬레이션 엔진부 - 저장장치 물리 모델 | `backend/simulator/storage_models.py` |
| 물리 시뮬레이션 엔진부 - 시계열 마모 연산 엔진 | `backend/simulator/engine.py` |
| 합성 데이터 생성부 | `backend/ai/model.py` 내 `generate_synthetic_dataset()` |
| 기계 학습 외삽 추론부 | `backend/ai/model.py` 내 `predict_rul_telemetry()` |
| API 게이트웨이부 | `backend/main.py` |
| 사용자 인터페이스부 + 시각화 출력부 | `frontend/src/App.jsx` |
| 시스템 도면 | `ARCHITECTURE_DIAGRAMS.md` |
