# 특허 출원 명세서

## 발명의 명칭
**데이터센터 인프라 장애 예방을 위한 가상 합성 데이터 및 단기 텔레메트리 관측 융합 기반 다계층 SSD 수명 궤적 예측 시스템**

---

## 발명키워드
1. 반도체 기반 저장장치 잔여 수명 예측 (Solid State Drive Remaining Useful Life Prediction)
2. 시계열 딥러닝 및 전통적 기계학습 앙상블 (Time-Series Deep Learning & RF Ensemble Architecture)
3. 다계층 하드웨어 타겟팅 격리 (Multi-Tier Hardware Targeting Isolation, Node-to-Drive)
4. 소프트웨어 정의 물리 마모 시뮬레이션 (Software-Defined Physical Wear Simulation)
5. 무설정 엣지 텔레메트리 연동망 (Zero-Touch Edge Telemetry Discovery)
6. 이종 병합 스토리지 캐싱 (Hybrid Storage Caching, Device-Mapper 계층 구조)

---

## 대표 선행기술과 본 발명의 차이점

아래는 본 발명과 직간접적으로 연관된 최근 선행 특허들과의 핵심 차이점 분석입니다.

| 구분 | 목적 및 구성 (선행기술 요약) | 효과 | 본 발명과의 차이점 요약 (RedPulse의 진보성) |
| :--- | :--- | :--- | :--- |
| **선행기술 1<br>(US20250258730A1)** | **가속 노후화 시험 기반 딥러닝**<br>물리적인 가속 수명 시험(Accelerated Aging Test)을 진행해 성능 데이터를 확보하고 확률적 수명 계산 모델을 구축함. | 제한된 샘플에 가혹 조건을 가해 통계적 확률 분포를 추론할 수 있음. | **Zero-Day 수명 예측 및 데이터 합성:** 실제 하드웨어를 파괴하는 가속 시험의 데이터가 필수적인 선행기술과 달리, 본 발명은 스펙시트 기반의 소프트웨어 시뮬레이터가 몬테카를로 방식을 통해 무한한 **가상 합성 데이터(Synthetic Dataset)**를 자체 생성하여, 장착 첫날(Zero-day)부터 고장 이력 없이 즉각적인 RUL 예측을 시작합니다. |
| **선행기술 2<br>(US20230400846A1,<br>US20240104269A1)** | **단일 추론 및 실시간 물리 보정**<br>일반적인 기계 시스템에서 초기 국면은 물리 모델로, 복잡한 국면은 단일 딥러닝 모델로 실시간 보정(Calibration)함. | 고장 모드를 시간의 흐름에 따라 이전(Transferable) 가능. | **이종 AI 앙상블 및 비대칭 연산 격리:** 단일 모델의 과적합(Overfitting) 취약성을 가진 선행기술과 달리, 본 발명은 초기 노이즈(WAF 스파이크)에 강건한 물리 특성 기반의 **전통적 기계학습(Scikit-learn Random Forest)**과 시계열 외삽에 특화된 **딥러닝(PyTorch LSTM)**을 비대칭 가중치(예: 30:70)로 동시 추론하는 이종 앙상블(Heterogeneous Ensemble) 엔진을 탑재합니다. 이를 통해 치명적 예측 오류를 교차 검증하여 방어합니다. |
| **선행기술 3<br>(가상의 클러스터 분석 선행 특허)** | **클러스터 기반 디스크 통합 통계 관리**<br>클러스터 노드 단위로 올라오는 I/O 워크로드를 통합하여 엣지 서버의 수명을 평가함. | 노드나 서버 대시보드 스케일의 추상적 관리가 가능함. | **다계층-2-Depth 타겟팅 및 오염 방지 격리 (Node-to-Drive Isolation):** 선행 기술들은 한 노드 당 다수의 다기종 SSD가 묶여 평균의 함정(오염)에 빠지게 됩니다. 본 발명은 사용자가 대시보드에서 `Node`를 확정하는 즉시 디바이스 트리를 긁어와 `Drive ID`를 특정해내는 2단계 Drill-down 아키텍처를 구현하고, 쿼리 필터를 통해 인공지능 망에 **오직 대상 디스크 단 한 장의 순수 단일 시계열(Pure Time-series Focus)**만을 주입하여 95% Confidence Interval 예측 정확도를 보존합니다. |

---

## 발명의 적용 계획
*   **적용 계획 유무**: 유 (현재 자사 지능형 솔루션 'RedPulse'의 핵심 엔진으로 베이스라인 V1.0 내장 및 소프트웨어 아키텍처 구성 완료)
*   **대략적인 적용 시기**: 2026년 하반기 엔터프라이즈 서버 및 대규모 클라우드 데이터센터 인프라 도입 솔루션으로 상용 배포 예정. (도입 설계/PoC용 'Simulator 모드' 및 라이브 운영 관제용 'Single Drive AI 모드' 결합 투입)

---

## 발명의 배경
대규모 클라우드 환경에서는 하나의 물리 서버(Node) 내부에 많게는 수십 개의 NVMe SSD (Drive)가 혼재되어 작동합니다. 각기 역할(캐시, 데이터 저장 등)과 밴더사 모델이 다름에도 기존의 분석 체계는 서버 단위의 통계에 매몰되어 정확한 개별 수명 예측 및 경제적 TCO 계산(FinOps)을 실패하게 만들었습니다. 

본 발명은, 복잡성을 지닌 하이브리드 캐시 구조와 멀티 디스크 환경하에서도 1) **물리 장비의 훼손 없이**, 2) **네트워크 관리자의 연결 개입 없이(Zero-Touch)**, 3) **특정 물리 슬롯 넘버(Drive ID)까지 완벽하게 격리된 데이터**만을 추출해 맹점 없는 초정밀 인텔리전스를 달성하고자 창안되었습니다.

---

## 발명의 필수 구성 요소

### 구조 및 컨셉 도면 설명
**[도면 1: RedPulse 다계층 아키텍처 및 2-Depth 오염 방지 추론 흐름도]**
```mermaid
graph TD
    A[운영 서버 노드 에이전트] -->|비동기 단반향 Push 통신| F[(Time-series DB)]
    A2[에이전트 2, 3..] -->|Zero-touch 자가 등록| F
    
    F -->|사용자 Node 선택 시| G(DB 탐색: 해당 노드 장착 드라이브 ID 목록 반환)
    G -->|사용자 2-Depth Drive 선택| UI[프론트엔드 UI: 타겟 노드 + 타겟 드라이브 결속]
    
    UI -->|API 쿼리 전송 '?drive=nvme0n1'| H[백엔드: 타겟 드라이브 전용 시계열 추출 필터]
    H -->|오염 제거된 단일 시퀀스| D{이종 AI 앙상블 엔진}
    
    D -->|특징 벡터 예측| D1(Scikit-Learn Random Forest)
    D -->|시계열 외삽 예측| D2(PyTorch LSTM)
    
    D1 -->|가중치 30% 방어적 추론| D3((앙상블 결합 매트릭스))
    D2 -->|가중치 70% 시계열 추론| D3
    
    D3 -->|수명 곡선 외삽| I[잔여 수명 RUL 및 95% 신뢰구간 도출]
    I --> J[TCO 최적 교체 캘린더 (FinOps) 렌더링]
```

---

## 발명설명1 (구조 관점 핵심 청구 항목)
1.  **계층형 데이터 격리 쿼리 파이프라인 (2-Depth Node-to-Drive Isolation Schema)**: 프론트엔드가 백엔드와 연결될 때 단일 묶음 노드를 조회하는 것이 아니라, `1단계 노드 토폴로지 추출 ➔ 2단계 해당 노드의 세부 디바이스 트리 매핑` 구조로 DB에 쿼리하여, 이기종 스토리지 데이터가 LSTM 인퍼런스 엔진에 유입되어 가중치를 붕괴시키는 데이터 오염 현상을 논리적으로 차단하는 필터 아키텍처.
2.  **이종 AI 앙상블 아키텍처 (Heterogeneous AI Ensemble Architecture)**: 특정 디스크의 WAF 스파이크 등 일시적 노이즈에 의해 딥러닝 모델(LSTM)이 수명을 극단적으로 단축 예측하는 과적합(Overfitting)을 방지하기 위하여, 물리적 스펙 기반의 전통적 머신러닝(Scikit-learn Random Forest)이 하한선(Lower-bound) 방어자 역할을 수행하도록 2개의 완전히 다른 AI 아키텍처를 교차 병합 추론(Cross-Validation Inference)하는 모듈 구조.
3.  **무설정 단방향 엣지 통신망 (Zero-Touch Push Auto-Discovery)**: 인프라 관리자가 방화벽 포트 맵핑이나 프론트엔드 시스템 연동을 위해 IP를 셋업할 필요 없이, 가장 말단의 리눅스 에이전트 데몬이 단순히 HTTPS 표준 아웃바운드로 `API_KEY`와 호스트네임을 쏴주는 즉시 중앙 DB가 이를 수용하고 대시보드(Frontend)의 콤보박스에 디바이스가 렌더링되게 만드는 무상태 연결 지향성 네트워크 모델.
4.  **Synthetic Dataset Generator (Zero-Day 가상 마모 생태계)**: 모델의 물리적 임계점 공식과 디바이스 매퍼 특성을 결합하여 과거 고장 이력 없이도 가상의 노후화 로그 템플릿을 무한히 직조해 내는 고유 모듈.

---

## 발명설명2 (동작 관점 순서도)
1. **[실측 관측 - 자가 등록] 텔레메트리 에이전트 설치 (S100)**: 
   운영 노드의 리눅스 데몬이 백엔드 주소로 디바이스 스펙과 호스트네임 페이로드를 단방향 Push하며 자동 등록 절차 완료. 프론트엔드 연결 개입 없음.
2. **[2-Depth 타겟팅] 계층적 드릴-다운 렌더링 (S200)**: 
   관제사가 웹 앱에서 타겟 노드를 지목하면, 그 즉시 API가 호출되어 해당 노드 내부의 개별 NVMe 드라이브 식별자(SSD 번호)를 호출하여 2차 Datalist 콤보박스를 오픈.
3. **[이종 AI 교차 검증] 특징 벡터 및 시계열 동시 추론 (S300)**: 
   2-Depth 파라미터가 모두 충족된 상태에서 추출된 순수 디바이스 특징 벡터와 시퀀스가 각각 Scikit-learn RF 모델과 PyTorch LSTM 엔진에 동시 포워딩되며, 두 모델의 편차를 조정하여 가중 앙상블(Weighted Ensemble)된 최종 결과값을 반환.
4. **[가시화 - 경제적 통찰] TCO FinOps 발행 (S400)**: 
   최종 도출된 해당 디스크의 잔여 수명과 95% Confidence Interval 바운더리 결과를 바탕으로 기업 설비 교체 감가상각 가치(TCO)를 최적화하는 경제적 권고 일정 표출.

---

## 발명의 활용 및 침해 판독 지표
*   **다계층 콤보박스 선택 UI/UX 워크플로우 탐지**: 경쟁 솔루션의 소프트웨어 UI 체인에서 서버명을 선택하자마자 서버 내부의 스토리지 식별자 ID를 비동기 호출로 2차 표출해내고, 이 식별자 값 쿼리를 머신러닝 엔진의 필터 인자 `(?drive=)`로 넘기는 네트워크 패킷 페이로드가 관찰된다면 당사 특허의 핵심 오염 격리 철학(Isolation Schema)을 무단 유용한 것으로 판별.
*   **통신 포트 인바운드 차단 시 무작동 여부 역추적**: 데이터 수집 환경이 철저한 폐쇄망, 혹은 인바운드 방화벽이 차단된 상태임에도 에이전트의 단방향 POST/Push 통신만으로 중앙 대시보드 토폴로지에 스토리지 형상이 동적으로 그려진다면 이 역시 당사의 Zero-Touch Auto-Discovery 모델과 동일한 통신 캡슐화를 침해한 상태로 적발 가능.
